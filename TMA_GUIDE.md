# 🏆 دليل تطبيق تيليجرام (LiveMatch TMA v2.0)

هذا الدليل الشامل يشرح كيفية عمل تطبيق تيليجرام الاحترافي وكيفية إعداده ورفعه.

---

## 📱 نظرة عامة على التطبيق

التطبيق الجديد (**v2.0**) هو تطبيق تيليجرام مصغر (Telegram Mini App) احترافي يتميز بـ:

### ✨ الميزات الرئيسية

| الميزة | الوصف |
|--------|-------|
| **🎯 عرض ذكي للمباريات** | عرض مباشر للمباريات الحية والقادمة مع ترقيم واضح |
| **🔍 بحث متقدم** | البحث عن الفرق والبطولات بسهولة |
| **⭐ المفضلة** | إضافة الفرق المفضلة لمتابعتها |
| **🏆 تصفية حسب البطولات** | تصفية المباريات حسب الدوري أو الدولة |
| **📺 اختيار السيرفر** | واجهة جميلة لاختيار سيرفر البث |
| **🔄 تحديث تلقائي** | التحديث التلقائي كل 5 دقائق |
| **📲 روابط مباشرة (Deep Linking)** | فتح مباريات محددة مباشرة |
| **💬 إشعارات تيليجرام** | استخدام نظام إشعارات تيليجرام |
| **🎨 تصميم متجاوب** | يعمل بشكل ممتاز على جميع الأجهزة |
| **💰 نظام إعلانات متقدم** | إعلانات ذكية لزيادة الأرباح |

---

## 📂 هيكل الملفات

```
sitefoot/
├── public/
│   ├── tma.html          ← الصفحة الرئيسية للتطبيق
│   ├── js/
│   │   └── tma-v2.js     ← كود التطبيق الجديد (v2)
│   ├── data/
│   │   └── matches.json  ← بيانات المباريات (المصدر الوحيد)
│   └── css/
│       └── simple-style.css
├── scrapers/             ← ملفات جلب البيانات
└── TMA_GUIDE.md         ← هذا الملف
```

---

## 🔧 كيفية عمل الكود

### 1. جلب البيانات
```javascript
// جلب مباريات من matches.json
const response = await fetch('data/matches.json?t=' + Date.now());
const data = await response.json();
state.matches = data.matches || [];
```

### 2. نظام التصفية
- **التبويبات**: الكل، المباشرة، القادمة، المفضلة
- **البطولات**: عرض جميع البطولات المتاحة
- **البحث**: البحث النصي في الفرق والبطولات

### 3. فتح السيرفرات
عند النقر على بطاقة مباراة:
1. عرض إعلان (Interstitial)
2. فتح واجهة اختيار السيرفر
3. عرض سيرفرات البث المتاحة

### 4. تشغيل البث
عند اختيار السيرفر:
1. عرض إعلان (Popup)
2. فتح المشغل
3. إمكانية تكبير الشاشة

---

## 💰 نظام الإعلانات (Monetag)

### أنواع الإعلانات المدمجة

| النوع | وقت العرض | الوصف |
|--------|-----------|-------|
| **In-App Interstitial** | كل 30 ثانية | إعلان بيني يظهر تلقائياً |
| **Interstitial** | عند النقر على بطاقة match | إعلان قبل اختيار السيرفر |
| **Popup** | عند اختيار السيرفر | إعلان قبل فتح البث |

### إعدادات Monetag

```javascript
// Zone ID
const CONFIG = {
    AD_ZONE: '10621765',
};
```

### تفعيل الإعلانات

```javascript
function triggerAd(type, callback) {
    const adFunction = window['show_10621765'];
    
    if (typeof adFunction === 'function') {
        try {
            if (type === 'interstitial') {
                adFunction({ type: 'inApp' });
            } else if (type === 'popup') {
                adFunction('pop');
            }
            setTimeout(callback, 500);
        } catch (e) {
            console.warn('Ad error:', e);
            callback();
        }
    } else {
        callback();
    }
}
```

---

## 🤖 إعدادات البوت (BotFather)

### الأوامر المطلوبة

1. **إنشاء بوت جديد**:
   ```
   /newbot
   ```

2. **إعداد اسم البوت**:
   - الاسم: `Live Match Today`
   - اليوزر: `@livematchtoday_bot` (يجب أن ينتهي بـ bot)

3. **تفعيل Mini App**:
   ```
   /mybots
   ```
   ثم اختر البوت → Bot Settings → Menu Button → Configure Menu Button

4. **إضافة زر القائمة**:
   - Title: "🏆 مباشر"
   - URL: `https://livematch-991.pages.dev/tma.html`

---

## 🔗 الروابط

### رابط التطبيق المباشر
```
https://t.me/livematchtoday_bot/live
```

### رابط مباراه محددة (Deep Link)
```
https://t.me/livematchtoday_bot/live?startapp=MATCH_ID
```

مثال:
```
https://t.me/livematchtoday_bot/live?startapp=414220606
```

---

## 📤 رفع على GitHub

### الطريقة الأولى: من خلال VS Code

1. **فتح المستودع**:
   ```bash
   cd c:/Users/uers/Desktop/sitefoot
   ```

2. **إضافة الملفات**:
   ```bash
   git add .
   ```

3. **كتابة التغييرات**:
   ```bash
   git commit -m "Add Telegram Mini App v2.0 with advanced features"
   ```

4. **الرفع**:
   ```bash
   git push origin main
   ```

### الطريقة الثانية: من خلال GitHub CLI

```bash
gh repo create livematch-tma --public --source=. --push
```

---

## 🌐 نشر على Cloudflare Pages

### الخطوات:

1. **دخول إلى Cloudflare Pages**:
   - اذهب إلى https://dash.cloudflare.com/pages

2. **إنشاء مشروع جديد**:
   - اضغط "Create a project"
   - اختر "Connect to Git"

3. **ربط المستودع**:
   - اختر المستودع الذي رفعته

4. **إعدادات البناء**:
   - Build command: (اتركه فارغاً)
   - Build output directory: `/public`
   - Root directory: `/`

5. **النشر**:
   - اضغط "Save and Deploy"

6. **الحصول على الرابط**:
   - بعد النشر، ستحصل على رابط مثل:
   - `https://livematch-xxx.pages.dev`

---

## 🔄 تحديث البيانات

### ملف scraper التلقائي

المشروع يحتوي على نظام scraper يعمل كل 5 دقائق لتحديث بيانات مباريات:

```javascript
// في scrapers/scraper_manager.js
const SCRAPE_INTERVAL = 5 * 60 * 1000; // 5 دقائق
```

### تشغيل الـ Scraper

```bash
# تشغيل يدوي
npm run scrape

# أو تشغيل كخدمة
pm2 start scrapers/scraper_manager.js
```

---

## 🎨 التخصيص

### تغيير الألوان

عدل في `tma.html` في قسم CSS:

```css
:root {
    --tg-accent: #1d9bf0;        /* لون التمييز */
    --live-red: #f4212e;         /* لون المباشر */
    --success-green: #00ba7c;    /* لون النجاح */
}
```

### تغيير معلومات البوت

عدل في `TMA_GUIDE.md`:

```markdown
- **اسم البوت:** Live Match Today
- **يوزر البوت:** @your_bot_username
- **رابط التطبيق:** https://your-domain.pages.dev/tma.html
```

---

## 🐛 حل المشاكل

### مشكلة: لا تظهر المباريات

1. تأكد من وجود ملف `matches.json` في `public/data/`
2. تأكد من أن الملف يحتوي على بيانات صحيحة
3. افتح أدوات المطور (F12) للتحقق من الأخطاء

### مشكلة: الإعلانات لا تظهر

1. تأكد من صحة Zone ID
2. تحقق من الاتصال بالإنترنت
3. جرب إصدارات مختلفة من Monetag SDK

### مشكلة: البث لا يعمل

1. تأكد من صحة روابط السيرفرات
2. جرب سيرفر آخر
3. تأكد من دعم الموقع للتشغيل

---

## 📋 قائمة المهام (TODO)

- [x] إنشاء تطبيق TMA v2.0
- [x] إضافة نظام البحث والتصفية
- [x] إضافة نظام المفضلة
- [x] إضافة نظام الإعلانات
- [x] إضافة Deep Linking
- [x] إضافة التحديث التلقائي
- [ ] إضافة إشعارات Push
- [ ] إضافة دعم اللغة الإنجليزية
- [ ] إضافة وضع ليلي/فاتح

---

## 📞 الدعم

للأسئلة أو المشاكل:
- GitHub Issues: https://github.com/yourusername/sitefoot/issues

---

*تم إنشاء هذا الدليل تلقائياً - LiveMatch TMA v2.0*
